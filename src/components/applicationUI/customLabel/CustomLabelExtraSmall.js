import styled from "styled-components";

const CustomLabelExtraSmall = styled.div`
  color: var(--elementDim);
  font-size: 11.5px;
  font-weight: 600;
  text-transform: capitalize;
`;

export default CustomLabelExtraSmall;
