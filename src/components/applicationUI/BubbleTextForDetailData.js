import styled from "styled-components";

const BubbleTextForDetailData = styled.div`
  padding: 10px 15px;
  background: var(--surface);
  font-size: 14px;
  font-weight: 700;
  border-radius: 5px;
  /* flex: 2; */

  color: var(--element);
  display: flex;
  justify-content: center;
  align-items: center;
  text-transform: capitalize;
`;

export default BubbleTextForDetailData;
