import styled from "styled-components";

const CustomLabelSmall = styled.div`
  color: var(--elementDim);
  font-size: 15.5px;
  font-weight: 600;
  text-transform: capitalize;
`;

export default CustomLabelSmall;
